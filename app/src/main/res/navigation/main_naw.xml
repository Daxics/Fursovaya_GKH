<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_naw"
    app:startDestination="@id/calculate">

    <fragment
        android:id="@+id/calculate"
        android:name="com.example.kursovaya.Calculate.Calculate"
        android:label="fragment_calculate"
        tools:layout="@layout/fragment_calculate" />
    <fragment
        android:id="@+id/scanner"
        android:name="com.example.kursovaya.scanner.Scanner"
        android:label="fragment_scanner"
        tools:layout="@layout/fragment_scanner" />
    <fragment
        android:id="@+id/history"
        android:name="com.example.kursovaya.History.History"
        android:label="fragment_calendar"
        tools:layout="@layout/fragment_history" />
</navigation>